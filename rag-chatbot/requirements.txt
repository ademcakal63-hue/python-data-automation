numpy>=1.24
scikit-learn>=1.3
pdfplumber>=0.10
streamlit>=1.30

# optional — better generation (auto-detected if the key is set):
anthropic>=0.40
openai>=1.30

# optional — local neural embeddings, no API key (uncomment to use):
# sentence-transformers>=2.2
